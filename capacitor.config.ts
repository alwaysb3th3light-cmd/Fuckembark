import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.transit.ticket',
  appName: 'Transit Pass',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
